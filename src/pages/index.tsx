import { css } from '@emotion/core';

export default function Home() {
  return (
    <div
      css={css`
        padding: 32px;
        background-color: #ccc;
      `}
    >
      homepage
    </div>
  );
}
